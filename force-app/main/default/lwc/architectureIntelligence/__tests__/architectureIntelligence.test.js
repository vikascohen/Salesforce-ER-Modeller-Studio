import { analyseArchitecture } from 'c/architectureIntelligence';
describe('architecture intelligence',()=>{it('calculates graph metrics without security concerns',()=>{const m={entities:[{name:'Account',fields:[{name:'Name'}]},{name:'Contact',fields:[{name:'AccountId',isRelationship:true}]},{name:'Island__c',fields:[]}],relationships:[{childEntity:'Contact',childField:'AccountId',parentEntity:'Account',kind:'lookup'}]};const a=analyseArchitecture(m);expect(a.entityCount).toBe(3);expect(a.relationshipCount).toBe(1);expect(a.lookupCount).toBe(1);expect(a.islands.map(x=>x.name)).toContain('Island__c');expect(a.nodes[0].degree).toBe(1);});it('detects cycles',()=>{const m={entities:['A','B','C'].map(name=>({name,fields:[]})),relationships:[['A','B'],['B','C'],['C','A']].map(([childEntity,parentEntity])=>({childEntity,parentEntity,childField:'x',kind:'lookup'}))};expect(analyseArchitecture(m).cycles.length).toBeGreaterThan(0);});});